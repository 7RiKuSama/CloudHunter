export interface ThemeProps {
    theme: {
        bg: string;
        color: string;
        boxColor: string;
        boxBg: string;
        borderColor: string;
        secondColor: string;
        isEnabled: boolean
    }
}

export interface ColorProps {
    danger: string
    hoverText: string
    hoverLight: string
}